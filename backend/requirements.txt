# File: requirements.txt
# Purpose: Pinned backend dependencies — prevents LangChain version churn breaking the app
# Step: Cross-cutting

flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1

pymupdf==1.24.5

langchain==0.2.6
langchain-text-splitters==0.2.2
langchain-community==0.2.6
langchain-groq==0.1.6

sentence-transformers==3.0.1
chromadb==0.5.3

gunicorn==22.0.0   # production WSGI server — Render uses this instead of Flask dev server
